### NOTE: you may need to install some packages as you go. If your program is not running, it
### may mean that you don't have a certain package to do it.
install.packages("mlbench")
install.packages('car')
library(mlbench)
# list the contents of the library
#library(help = "mlbench")
library("ggplot2")                     # Load ggplot2 package
library("GGally")                      # Load GGally package
library(lmtest)
library(corrplot)
library(car)
# 1. Make sure that you place your dataset in the same folder where the R file is saved
# 2. Go to Session-> Set Working Directory-> To Source File Location
# 3. Read in your data. On the right hand side window click and check your data
data=read.csv('df2.csv')
setwd("~/code/cse4334_datamining/HW4")
### NOTE: you may need to install some packages as you go. If your program is not running, it
### may mean that you don't have a certain package to do it.
install.packages("mlbench")
install.packages('car')
library(mlbench)
# list the contents of the library
#library(help = "mlbench")
library("ggplot2")                     # Load ggplot2 package
library("GGally")                      # Load GGally package
library(lmtest)
library(corrplot)
library(car)
# 1. Make sure that you place your dataset in the same folder where the R file is saved
# 2. Go to Session-> Set Working Directory-> To Source File Location
# 3. Read in your data. On the right hand side window click and check your data
data=read.csv('df2.csv')
install.packages("mlbench")
install.packages("car")
### NOTE: you may need to install some packages as you go. If your program is not running, it
### may mean that you don't have a certain package to do it.
install.packages("mlbench")
install.packages('car')
library(mlbench)
# list the contents of the library
#library(help = "mlbench")
library("ggplot2")                     # Load ggplot2 package
library("GGally")                      # Load GGally package
library(lmtest)
library(corrplot)
library(car)
# 1. Make sure that you place your dataset in the same folder where the R file is saved
# 2. Go to Session-> Set Working Directory-> To Source File Location
# 3. Read in your data. On the right hand side window click and check your data
data=read.csv('df2.csv')
View(data)
View(data)
# 4. You will notice that an extra column was created for the index, we need to remove it
df = subset(data, select = -c(X) )
head(df)
head(df)
model1=lm(price~., data=df)
### NOTE: you may need to install some packages as you go. If your program is not running, it
### may mean that you don't have a certain package to do it.
install.packages("mlbench")
install.packages('car')
library(mlbench)
# list the contents of the library
#library(help = "mlbench")
library("ggplot2")                     # Load ggplot2 package
library("GGally")                      # Load GGally package
library(lmtest)
library(corrplot)
library(car)
# 1. Make sure that you place your dataset in the same folder where the R file is saved
# 2. Go to Session-> Set Working Directory-> To Source File Location
# 3. Read in your data. On the right hand side window click and check your data
data=read.csv('df2.csv')
# 4. You will notice that an extra column was created for the index, we need to remove it
df = subset(data, select = -c(X) )
install.packages("mlbench")
### NOTE: you may need to install some packages as you go. If your program is not running, it
### may mean that you don't have a certain package to do it.
install.packages("mlbench")
install.packages('car')
library(mlbench)
# list the contents of the library
#library(help = "mlbench")
library("ggplot2")                     # Load ggplot2 package
library("GGally")                      # Load GGally package
library(lmtest)
library(corrplot)
library(car)
setwd("~/code/cse4334_datamining/HW4")
data=read.csv('df2.csv')
df = subset(data, select = -c(X) )
head(df)
data=read.csv('df2.csv')
df = subset(data, select = -c(X) )
head(df)
### NOTE: you may need to install some packages as you go. If your program is not running, it
### may mean that you don't have a certain package to do it.
install.packages("mlbench")
install.packages('car')
library(mlbench)
# list the contents of the library
#library(help = "mlbench")
library("ggplot2")                     # Load ggplot2 package
library("GGally")                      # Load GGally package
library(lmtest)
library(corrplot)
library(car)
# 1. Make sure that you place your dataset in the same folder where the R file is saved
# 2. Go to Session-> Set Working Directory-> To Source File Location
# 3. Read in your data. On the right hand side window click and check your data
data=read.csv('df2.csv')
# 4. You will notice that an extra column was created for the index, we need to remove it
df = subset(data, select = -c(X) )
install.packages("mlbench")
### NOTE: you may need to install some packages as you go. If your program is not running, it
### may mean that you don't have a certain package to do it.
install.packages("mlbench")
install.packages('car')
library(mlbench)
# list the contents of the library
#library(help = "mlbench")
library("ggplot2")                     # Load ggplot2 package
library("GGally")                      # Load GGally package
library(lmtest)
library(corrplot)
library(car)
# 1. Make sure that you place your dataset in the same folder where the R file is saved
# 2. Go to Session-> Set Working Directory-> To Source File Location
# 3. Read in your data. On the right hand side window click and check your data
data=read.csv('df2.csv')
# 4. You will notice that an extra column was created for the index, we need to remove it
df = subset(data, select = -c(X) )
data=read.csv('df2.csv')
# 4. You will notice that an extra column was created for the index, we need to remove it
#df = subset(data, select = -c(X) )
# you can always look at your data either going to the right upper window,
# or calling the head() function
head(df)
data=read.csv('df2.csv')
# 4. You will notice that an extra column was created for the index, we need to remove it
#df = subset(data, select = -c(X) )
# you can always look at your data either going to the right upper window,
# or calling the head() function
head(df)
data=read.csv('df2.csv')
# 4. You will notice that an extra column was created for the index, we need to remove it
#df = subset(data, select = -c(X) )
# you can always look at your data either going to the right upper window,
# or calling the head() function
head(df)
data=read.csv('df2.csv')
# 4. You will notice that an extra column was created for the index, we need to remove it
#df = subset(data, select = -c(X) )
# you can always look at your data either going to the right upper window,
# or calling the head() function
head(df)
data=read.csv('df2.csv')
# 4. You will notice that an extra column was created for the index, we need to remove it
#df = subset(data, select = -c(X) )
# you can always look at your data either going to the right upper window,
# or calling the head() function
head(df)
head(df)
# 1. Make sure that you place your dataset in the same folder where the R file is saved
# 2. Go to Session-> Set Working Directory-> To Source File Location
# 3. Read in your data. On the right hand side window click and check your data
data=read.csv('df2.csv')
# 4. You will notice that an extra column was created for the index, we need to remove it
#df = subset(data, select = -c(X) )
# you can always look at your data either going to the right upper window,
# or calling the head() function
head(df)
df = subset(data, select = -c(X) )
df = subset(data, select = -c(X) )
View(data)
df = data
head(df)
model1=lm(price~., data=df)
View(data)
summary(model1)
summary(model1b)
model1b=lm(price~fuel_type+wheel_base+length+width+heights+curb_weight+engine_size+bore+stroke+comprassion+horse_power+peak_rpm+city_mpg+highway_mpg, data=df)
summary(model1b)
df_new = subset(df, select = c(fuel_type, wheel_base, width, heights, engine_size, stroke, horse_power, peak_rpm, highway_mpg, price))
model2=lm(price~., data=df_new)
summary(model2)
model2b->
# Compare your models with ANOVA. What do you find?
#If you used the alternative ways, make sure that you change the model names in the function
anova(model2, model1)
anova(model2, model1)
anova(model1, model2)
vif(model1)
vif(model2)
plot(model1)
plot(model2)
ggpairs(df)
library(MASS)
step.model_both <- step(model1, direction = "both",
trace = FALSE)
summary(step.model_both)
step.model_forward <- step(model1, direction = "forward",
trace = FALSE)
summary(step.model_forward)
#backward selection
step.model_backward <- step(model1, direction = "backward",
trace = FALSE)
summary(step.model_backward)
summary(model2)
summary(model1)
df_new = subset(df, select = c(fuel_type, width, heights, engine_size, stroke, horse_power, peak_rpm, highway_mpg, price))
model2=lm(price~., data=df_new)
summary(model2)
anova(model2, model1)
vif(model1)
vif(model2)
plot(model1)
plot(model2)
plot(model2)
plot(model1)
library(MASS)
# backward-forward selection
step.model_both <- step(model1, direction = "both",
trace = FALSE)
summary(step.model_both)
# forward selection
step.model_forward <- step(model1, direction = "forward",
trace = FALSE)
summary(step.model_forward)
#backward selection
step.model_backward <- step(model1, direction = "backward",
trace = FALSE)
summary(step.model_backward)
